"""
Giao diện Streamlit (LucenFace / chuẩn hóa chân dung).

- `frontend.app` — ứng dụng đầy đủ (lazy-load backend xử lý ảnh).
- `frontend.config` — hằng số build / theme.
- `frontend.image_io`, `frontend.thumbnails` — đọc ảnh upload/clipboard và xem trước.
- `frontend.styling` — CSS chung.
- `frontend.processor_service` — cache PortraitProcessor + gọi pipeline.
- `frontend.streamlit_helpers` — checklist, ZIP, secrets.
"""
